%h3
  = link_to result.name, person_path(result)
  %span.type (Student)

- if result.functionaries.count > 0
  %p!= result.functionaries.map { |f| link_to f.chapter_post.name, f.chapter_post }.to_sentence
