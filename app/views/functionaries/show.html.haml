%h2
  - if not @functionary.postponed
    = @functionary.person.try(:name)
    = "(#{@functionary.person.try(:kth_username)})"
  - else
    Vakantsatt,
  = @functionary.chapter_post.name

%p
  %b Period:
  = "#{@functionary.active_from} till #{@functionary.active_to}"

- side_link 'Tillbaka till funktionärsposter', functionaries_path
- side_link 'Redigera', edit_functionary_path(@functionary)
- side_link 'Ta bort', functionary_path, :confirm => 'Are you sure?', :method => :delete
