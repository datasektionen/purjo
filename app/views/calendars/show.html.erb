<% stylesheet 'calendar' %>

<h2>Kalender</h2>

<p>Här kan du hålla dig uppdaterad om vad som händer på sektionen, du kan även prenumerera på kalendern i <%= link_to 'ics', 'http://en.wikipedia.org/wiki/ICalendar' %>-format <%= link_to image_tag('calendar.png', :class => 'icon'), calendar_ics_path %> <%= link_to "här", calendar_ics_path %>.</p>

<ul id="navbar" class="horizontal">
  <li><%= previous_month_link(@month, @year) %></li>
  <li><%= get_month_name(@month) %> <%= @year %></li>
  <li><%= next_month_link(@month, @year) %></li>
</ul>

<%#= calendar(@posts, :first_day_of_week => 1, :show_week => true, :month => @month, :year => @year, :abbrev => 0..-1) %>

<% html = Ior::Calendar::HtmlRenderer.new(@posts, @year, @month) %>
<%= render :inline => html.render %>

