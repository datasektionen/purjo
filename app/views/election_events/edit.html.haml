%h2 Redigerar ett valtillfälle
- form_for(@election_event) do |f|
  %p
    = f.label :name
    <br />
    = f.text_field :name
  %p
    = f.label :date
    <br />
    = f.date_select :date
  %p
    = f.submit "Update"

- side_link 'Tillbaka till valtillfällen', election_events_path
- side_link 'Visa valtillfälle', @election_event
- side_link 'Ta bort valtilfälle', @election_event, :confirm => 'Are you sure?', :method => :deletes
